import React from "react";
import { View, Text, Button } from "react-native";

const NotiScreen = ({ changeScreen }) => {
  return (
    <View>
      <Text>Noti</Text>
    </View>
  );
};

export default NotiScreen;
